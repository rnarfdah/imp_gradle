defaultTasks 'DisplayTasks'

task emptyTask {
    doLast{
        //do something
    }
}

task DisplayTasks {
    doLast {
        tasks.each {
            println "Tak name is ${it.name}"
        }
    }
}